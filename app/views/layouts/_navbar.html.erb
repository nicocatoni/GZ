<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top ">
  <a class="text-success" href="<%= growshops_path %>">GROWZONE</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <% if user_signed_in? %>
        <a class="nav-link text-white" href="<%= new_growshop_path %>"> NuevoGrow! <span class="sr-only">(current)</span></a>
        <% else %>
          
          <% end %>
      </li>

      
      <li class="nav-item">
          <a class="nav-link  text-success" href="<%= assists_index_path %>" tabindex="-1" aria-disabled="true">¿ Necesitas Ayuda ?</a>
      </li>

      <li class="nav-item">
        <a class="nav-link  text-white" href="<%= pages_index_path %>" tabindex="-1" aria-disabled="true"></a>
      </li>
        

      <li class="nav-item dropdown ">
        <a class="nav-link dropdown-toggle text-success" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <% if user_signed_in? %>
            <%= current_user.username %>
            <% else %>
            Ingreso
          <% end %>
        </a>

      
        <div class="dropdown-menu bg-dark" aria-labelledby="navbarDropdown">
          <% if user_signed_in? %>
          <a class="dropdown-item text-success" href="<%= destroy_user_session_path %>" data-method="DELETE" >Salir</a>

          <% else %>
            <a class="dropdown-item text-success bg-dark" href="<%= new_user_session_path %>">Ingreso</a>
            <a class="dropdown-item text-success bg-dark" href="<%= new_user_registration_path %>">Registro</a>
          <% end %>
        </div>
        

      </li>
      

    </ul>

  </div>
</nav>